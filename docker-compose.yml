version: "3"
services:
  application:
    image: SpringAPP
    ports:
      - "4041:4041"
    networks:
      - springboot-db-net
    depends_on:
      - mysqldb
    volumes:
      - /data/springboot-app
    
  mysqldb:
    image: mysql:5.7
    networks:
      - springboot-db-net
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=emp
    volumes:
      - /data/mysql
networks:
  springboot-db-net: